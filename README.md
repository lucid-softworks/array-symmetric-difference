# @lucid-softworks/array-symmetric-difference

Return unique values present in exactly one of two inputs.

```ts
import { symmetricDifference } from "@lucid-softworks/array-symmetric-difference";

symmetricDifference([1, 2], [2, 3]); // [1, 3]
```
